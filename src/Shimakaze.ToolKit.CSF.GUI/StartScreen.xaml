<Fluent:StartScreen x:Class="Shimakaze.ToolKit.CSF.GUI.StartScreen"
  xmlns:Fluent="urn:fluent-ribbon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Shimakaze.ToolKit.CSF.GUI"
                    IsOpen="True" CloseOnEsc="False"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Fluent:StartScreenTabControl>
        <Fluent:StartScreenTabControl.LeftContent>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Start" Grid.Row="0" FontSize="48"
                           Foreground="{DynamicResource Fluent.Ribbon.Brushes.IdealForegroundColorBrush}" />
                <Grid Grid.Row="1" Margin="10 20 10 0">
                    <Grid.Resources>
                        <Style TargetType="Button" BasedOn="{StaticResource ButtonAccentRevealStyle}">
                            <Setter Property="FontSize" Value="18"/>
                            <Setter Property="Margin" Value="5"/>
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="BorderThickness" Value="2"/>
                        </Style>
                    </Grid.Resources>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    <StackPanel>
                        <Button Content="打开" />
                        <Button Content="新建" />
                        <Button Content="导入" />
                    </StackPanel>
                    <StackPanel Grid.Row="1">
                        <Button Content="退出" />
                    </StackPanel>
                </Grid>
            </Grid>
        </Fluent:StartScreenTabControl.LeftContent>

        <Fluent:StartScreenTabControl.RightContent>
            <!--<Image Source=""/>-->
            <StackPanel Orientation="Vertical">
                <TextBlock VerticalAlignment="Center"
                           HorizontalAlignment="Center">
                  You can close the start screen by either clicking the button below or by pressing ESC
                </TextBlock>
                <Fluent:Button HorizontalAlignment="Center"
                               IsDefinitive="True">
                    Close start screen
                </Fluent:Button>
            </StackPanel>
        </Fluent:StartScreenTabControl.RightContent>
    </Fluent:StartScreenTabControl>
</Fluent:StartScreen>
